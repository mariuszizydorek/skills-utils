# Release Notes Snippet

Use this snippet in the next release/changelog entry:

```md
## Docs and GitHub Pages

- Added a dedicated private-repo troubleshooting guide in both Markdown (`docs/troubleshooting.md`) and styled HTML (`docs/troubleshooting.html`).
- Added a docs landing page (`docs/index.html`) with quick links to troubleshooting and manifest-schema docs.
- Added a GitHub Pages deployment workflow (`.github/workflows/pages.yml`) that publishes `docs/` on pushes to `main`.
- Added cross-links between README/docs and included GitHub Pages setup instructions.
```
